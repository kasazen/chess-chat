# Project Configuration: Chess Chat

## Git Environment
- **Remote Origin**: `https://github.com/kasazen/chess-chat.git`
- **Primary Branch**: `main`
- **Verification**: Run `git remote -v` to confirm linkage before pushing.

## Standard Commit Workflow
1. **Stage**: Add changed files related to the task in `postbox/todo.md`.
2. **Review**: Ensure changes pass the Gemini Pre-Commit Audit.
3. **Commit**: Use Conventional Commits (e.g., `feat:`, `fix:`, `refactor:`).
4. **Push**: Always push to `origin main` after a successful local commit.

## Authorized Committers
- **Claude Code** (Performer)
- **Gemini CLI** (Inspector/Auditor)